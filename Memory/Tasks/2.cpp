/*
### **Task 2: Array Allocation with new[] / delete[]**
**Concept to Learn:** `new[]` vs `new`, `delete[]` requirement, array on heap.

**Instructions:**
Allocate an array of 5 `int`s on heap using `new[]`.
Initialize them with values 10, 20, 30, 40, 50.
Print the array and addresses of each element.
Deallocate with `delete[]` and set pointer to `nullptr`.

**Expected Output:**
```
Values: 10 20 30 40 50
Addresses: 0x... 0x... ...
```

**Verification Questions:**
1. What happens if you use `delete` instead of `delete[]`?
2. Why are the addresses consecutive?

*/
