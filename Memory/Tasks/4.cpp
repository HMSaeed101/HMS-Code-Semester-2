/*
### **Task 4: Detecting Memory Leaks**
**Concept to Learn:** What a memory leak is and how it happens in functions/loops.

**Instructions:**
Write a function `void createLeak()` that allocates an array of 1000 `int`s but never deletes it.
Call this function 10 times from `main`.
Add a comment explaining how you would detect this leak in real projects (tools).

**Verification Questions:**
1. Does the program crash? Why or why not?
2. How much memory is leaked after 10 calls (rough calculation)?
*/
